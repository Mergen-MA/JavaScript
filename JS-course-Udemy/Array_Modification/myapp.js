var array = [
    "string",
    100,
    ["embed", 200],
    { car: "ford"},
    function() { return "drive"}

];

// Javascript Methods

// shift() - removes the first element of an array
// unshift() - add elements into the arrary
// Pop() - removes the last element of an array
// Splice() - removes elements from where we want ArrayBuffer.
// splice(2,2, 300, 129) : from the 2nd element remove 2 and add the subsequent elements.